[defaults]

# Use the repository inventory as the default inventory source
inventory = ./inventory/hosts.ini

# Automatically detect the appropriate Python interpreter
interpreter_python = auto_silent

# Disable retry files for a cleaner repository
retry_files_enabled = False

# Keep output readable
deprecation_warnings = False
